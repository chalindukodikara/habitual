apiVersion: openchoreo.dev/v1alpha1

metadata:
  name: habitual-ui

endpoints:
  - name: http
    port: 3000
    type: HTTP
    visibility:
      - external

dependencies:
  endpoints:
    - component: habitual-api
      name: http
      visibility: project
      envBindings:
        address: API_URL
        host: API_HOST
        port: API_PORT
